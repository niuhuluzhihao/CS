进程/线程   计算机设备需要合作

优点1：共享资源

优点2：加速

优点3：模块化


希望：
    无论多个线程的指令序列怎样交替执行，程序都必须正常工作

9.2 一些概念
系统缺陷：结果依赖于并发执行或者事件的顺序/时间
    不确定性
    不可重现
怎么避免竞态
    让指令不被打断

原子操作(atomic operation)

临界区:
    临界区指的是进程中一段需要访问资源并且另一个进程处于相应代码区域时候便不会被执行的代码区域

互斥：
    当一个进程处于临界区并访问呢共享资源的时候，没有其他进程会处于临界区并且访问任何相同的共享资源

死锁:
    两个或以上的进程，在相互等待完成特定任务，而最终没法将自身任务进行下去

饥饿：
    一个可执行的进程，被调度器持续忽略，以至于虽然处理可执行状态但是不被执行

9.3 一些概念

锁
解锁
死锁：A拿到锁1，B拿到锁2，A想继续拿到锁2后再继续执行，b想继续拿到锁1在继续执行。导致A和B谁也无法继续执行


9.4 一些概念
为标签添加标签会怎么样呢？--还是会导致没有线程去买面包
lock.acquire()
local.release()

9.5 临界区
互斥：同一个时间临界区最多存在一个线程
progress:如果一个线程想要进入临界区，那么他最终会成功
有限等待：如果一个线程处于入口区，那么在线程请求被接受之前，其他线程进入临界区的时间是有限制的

9.6 禁用硬件中断
    一旦中断被禁用，线程就无法被停止
    无法限制响应中断所需的时间
    要小心使用

9.7 基于软件的解决方案
    dekker算法
    bakery算法
    复杂：
        需要两个进程间的共享数据项
    需要忙等待：
        浪费cpu
    没有硬件保证的情况下无真正的软件解决方案