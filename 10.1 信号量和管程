
信号量是希望在临界区同时有多个进程或者线程进行操作，不做单个的限制

信号量:一个整形，两个原子操作
    p():sem减去1，如果sem<0,等待，否则继续
    v():sem加上1，如果semm<=0,唤醒一个等待的p   ---唤醒相当于通知操作


信号量是整数

信号量是被保护的变量

P能够阻塞，V不会阻塞 

两种类型信号量
    二级制信号量：可以是0或者1
    一般/计数信号量：可取任何非负值
    两者相互表现

信号量可以用在两个方面
    互斥
    条件同步（一个线程等待另一个线程的事情发生）


信号量的使用
    一个线程等待另一个线程处理事情
        比如生成或者消费东西
        互斥（锁机制）是不够的

    正确性要求：
        任何时间只能有一个线程操作缓冲区（互斥）
        当缓冲区为空，消费者必须等待生产者（调度、同步约束）
        当缓冲区满，生产者必须等待消费者（调度、同步约束）

    每个约束使用单独的信号量
        二进制信号量互斥  -----（对buffer的读取操作是互斥操作）
        一般信号量fullbuffers  ----通知机制---
        一般信号量emptybuffers ---buffer有没有满