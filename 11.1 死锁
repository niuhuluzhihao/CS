死锁

  流量只在一个方向
  桥的每个部分可以看做一个资源
  如果死锁，可能通过一辆车倒退解决
  发生死锁，可能几辆车都需要倒退
  可能发生饥饿


系统模型：
    资源
    进程使用资源
        请求资源
        使用资源
        释放资源

    可重复使用的资源
        在一个时间只能一个进程使用且不被删除
        进程获取资源，后来释放由其他资源重用
        处理器，I、O通道，主和副存储器
        如果每个进程拥有一个资源请求其他资源，死锁可能发生

    使用资源
        创建和销毁
        在I/O缓冲区的中断，信号，消息，信息
        如果接受消息阻塞可能会发生死锁
        可能少见的组合事件会引起死锁

    资源分配图

    如果没有循环，没有死锁
    如果有循环
        资源只有一个实例，死锁
        好几个实例，不会死锁

    