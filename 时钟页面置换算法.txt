时钟页面置换算法：LRU近似，对FIFO的一种改进

基本思路:
    需要用到页表项中的访问位，当一个页面被装入内存的时候，把该位初始化为0。
    然后如果这个页面被访问，则该位置为1。
    将各个页面组织成环形链表，把指针指向最老的页面
    当发生缺页中断时，考察指针所指向的最老页面，若它的访问页是0，立即淘汰。若访问位是1，则把该位置改为0.然后指针往下移动一格。
      命中指针不动，缺页指针下移

